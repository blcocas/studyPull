
HTTP?
hypertext transfer protocol
tim berners-lee

http란? html을 웹브라우저와 웹서버 사이에서 주고받기위해서는 규칙을 정의한것.
request와 response
웹브라우저에서 request를 하고 webserver에서 response를함.
이 작업을 chrome개발자도구 네트워크탭에서 확인할 수 있다.

cookie
쿠키? 웹사이트에 방문했을때 로그인을 하고 나중에 다시 접속하면 로그인이 유지되있는것...
이게바로 쿠키를 이용한것임.
http의 프로토콜은 상태가 없음.무슨의미냐면 접속할때마다 이전에 접속했던 상태같은것들이
없기때문에 유지되지 않으므로 나중에 접속했던 기록같은걸 알 수 없다는 뜻. 
넷스케이프에서 쿠키라는 기능을 만들었음. 이 기능을 이용하면 전에 접속했던 상태를 알 수
있음.
쿠키자체가 활용도가 높음. 쿠키를 기초로해서 세션이라는 주제가 나옴. 세션을 기반으로 해서
인증이 나오게됨. 쿠키를 활용해서 웹앱을 만들어보자.

express는 쿠키기능이 없음. 당연히 api찾아보고... 거기서 cookie-parser를 쓰라고 나와있으니
이 미들웨어 또한 가져와야한다.


쿠키의 보안?
이 req,res할때 전송되는 쿠키들의 값은 역시 req,res헤더에 다 나와있다. 그래서
예전에 돌핀인가? 그 전송데이터 캐치하는 그런 프로그램을 이용해서 누군가가 쿠키값을 
지켜볼 수도있게된다. 만약 그 쿠키가 로그인같은 경우라면? 매우 심각안 보안문제임.
그래서 우리는 https라는 통신방법을 써야한다. 혹은 쿠키값 자체를 암호화 할수 있다. 
것을 하기 위해선?? 
쿠키값 자체를 바꾸려면? cookie parser에 키값을 넣는다.
그리고 sigedCookies로 쓰면된다. 그리고 쿠키를 구울때 {singed : true}를 해줘야 함.


